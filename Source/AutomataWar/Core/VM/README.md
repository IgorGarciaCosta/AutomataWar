# VM — Automata Virtual Machine

Deterministic bytecode interpreter. One instruction dispatch per tick, no allocation in the hot path.
Emits fixed-size intents consumed by the simulation. No arena/opponent access.
